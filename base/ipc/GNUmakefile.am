OPTDIRS=

if CONFIG_RTAI_BITS
OPTDIRS += bits
endif

if CONFIG_RTAI_FIFOS
OPTDIRS += fifos
endif

if CONFIG_RTAI_SHM
OPTDIRS += shm
endif

if CONFIG_RTAI_SEM
OPTDIRS += sem
endif

if CONFIG_RTAI_MSG
OPTDIRS += msg
endif

if CONFIG_RTAI_MBX
OPTDIRS += mbx
endif

if CONFIG_RTAI_TBX
OPTDIRS += tbx
endif

if CONFIG_RTAI_MQ
OPTDIRS += mq
endif

if CONFIG_RTAI_NETRPC
OPTDIRS += netrpc
endif

SUBDIRS = $(OPTDIRS)
