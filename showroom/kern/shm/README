****** SHARED MEMORY EXAMPLE ******

This is a simple example on how use RTAI shared memory (rtai_shm). 
The kernel module "shm.o" increments periodically a counter located 
in shared memory and the application program "display" prints its
contents.

To run it do:

make
./run
